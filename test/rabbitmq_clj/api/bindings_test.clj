(ns rabbitmq-clj.api.bindings-test
  "Tests related to bindings endpoints."
  (:require [clojure.test :refer :all]
            [rabbitmq-clj.core :refer [dispatch]]))

(deftest bindings
  (testing "Bindings endpoints"
    (let []
      )))
