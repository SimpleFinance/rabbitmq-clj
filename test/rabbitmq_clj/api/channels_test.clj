(ns rabbitmq-clj.api.channels-test
  "Tests related to channels endpoints."
  (:require [clojure.test :refer :all]
            [rabbitmq-clj.core :refer [dispatch]]))

(deftest channels
  (testing "Channels endpoints"
    (let [])))
